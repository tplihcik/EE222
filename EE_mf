CC = gcc
DEBUG = -g
CFLAGS = -Wall -c $(DEBUG)
LFLAGS = -Wall $(DEBUG)


runProg : tempTable.o
	$(CC) $(LFLAGS) tempTable.o -o runProg

tempTable.o : tempTable.c
	$(CC) $(CFLAGS) tempTable.c

clean:
	\rm *.o runProg
